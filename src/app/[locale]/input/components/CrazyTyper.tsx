import type { FormDataType } from "../types"

interface CrazyTyperProps {
    step: number
    formData: FormDataType
}

// 주접 멘트 기능 비활성화 - 빈 컴포넌트로 대체
export function CrazyTyper(_props: CrazyTyperProps) {
    return null
}
